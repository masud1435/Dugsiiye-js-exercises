// Create a function named `add` that takes two parameters and returns their sum. Test the function by calling it with different arguments. (use arrow function)

const add = (a,b)=>{
    return a+b;
}

let sum = add (9,6);
console.log ("Sum=" + " " +sum); // output: 15